Next.js app — built in Phase 4. Scaffold with `npx create-next-app@latest` into this
folder when you get there, keeping this package name and the `@alphades/db` dependency.

Design: see the canvas (call card, feed, journal, public track record).
Type: Space Grotesk for UI, JetBrains Mono for every figure. Lime accent (#b8f24d) on
near-black (#0c0d10) — deliberately not StalkFun's emerald.
